_schema-version: "3.2"
ID: travelustabtravelapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travelustabtravelapp-destination-content
  type: com.sap.application.content
  requires:
  - name: travelustabtravelapp-destination-service
    parameters:
      content-target: true
  - name: travelustabtravelapp-repo-host
    parameters:
      service-key:
        name: travelustabtravelapp-repo-host-key
  - name: travelustabtravelapp-uaa
    parameters:
      service-key:
        name: travelustabtravelapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travelustabtravelapp_html_repo_host
          ServiceInstanceName: travelustabtravelapp-html5-srv
          ServiceKeyName: travelustabtravelapp-repo-host-key
          sap.cloud.service: travelustabtravelapp
        - Authentication: OAuth2UserTokenExchange
          Name: travelustabtravelapp_uaa
          ServiceInstanceName: travelustabtravelapp-xsuaa-srv
          ServiceKeyName: travelustabtravelapp-uaa-key
          sap.cloud.service: travelustabtravelapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travelustabtravelapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travelustabtravelapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travelustabtravelapp.zip
      name: travelustabtravelapp
      target-path: resources/
- name: travelustabtravelapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travelustabtravelapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travelustabtravelapp-destination-service
    service-plan: lite
- name: travelustabtravelapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travelustabtravelapp-xsuaa-srv
    service-plan: application
- name: travelustabtravelapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travelustabtravelapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
